# De rekenkern heeft niets nodig: alleen de standaardbibliotheek. Ook de
# tests draaien zonder installatie — tests/psqlschil.py praat met psql
# als er een database bij te pas komt.
#
# De webserver van stap 8 heeft wél een driver nodig:
#
#     pip install -r requirements.txt
#     python3 -m vakto.web
#
psycopg[binary]>=3.2
